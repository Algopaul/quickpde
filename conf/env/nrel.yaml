# @package _global_
defaults:
  - override /hydra/launcher: submitit_slurm
  - _self_

hydra:
  launcher:
    timeout_min: 60
    cpus_per_task: 32
    mem_gb: 32
    account: extremedata
    partition: shared
